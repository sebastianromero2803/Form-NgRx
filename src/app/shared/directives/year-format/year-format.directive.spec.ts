import { YearFormatDirective } from './year-format.directive';

describe('YearFormatDirective', () => {
  it('should create an instance', () => {
    const directive = new YearFormatDirective();
    expect(directive).toBeTruthy();
  });
});
